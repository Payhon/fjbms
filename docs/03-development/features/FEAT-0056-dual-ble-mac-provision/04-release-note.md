# 发布说明

## 变更

- 支持外挂蓝牙透传设备的双 MAC 添加流程。
- 后台电池列表新增“设备身份MAC”展示与搜索。
- BMS bridge 不再把协议身份 MAC 覆盖到连接用 `ble_mac`。

## 数据库

- 执行 `backend/sql/59.sql`。

## 回滚

- 代码回滚后旧版本不会读取 `identity_ble_mac`。
- 数据库字段可保留，不影响旧逻辑。
